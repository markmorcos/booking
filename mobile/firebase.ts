// Firebase is configured via native config files:
// - iOS: GoogleService-Info.plist
// - Android: google-services.json
//
// The @react-native-firebase/app plugin reads these automatically.
// No JavaScript-side configuration is needed for native Firebase.
//
// For web, you would add config here, but this app targets iOS/Android only.

export const FIREBASE_WEB_CLIENT_ID = 'YOUR_WEB_CLIENT_ID.apps.googleusercontent.com';
