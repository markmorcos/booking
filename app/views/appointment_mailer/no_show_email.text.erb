<%= t('appointment_mailer.no_show_email.title') %>
<%= "=" * t('appointment_mailer.no_show_email.title').length %>

<%= t('appointment_mailer.no_show_email.greeting', name: @appointment.booking_name) %>

<%= t('appointment_mailer.no_show_email.message') %>

<%= t('appointment_mailer.common.details') %>:
- <%= t('appointment_mailer.common.date') %>: <%= l(@appointment.availability_slot.starts_at.to_date, format: :long, locale: I18n.locale) %>
- <%= t('appointment_mailer.common.time') %>: <%= l(@appointment.availability_slot.starts_at, format: :default, locale: I18n.locale) %> - <%= l(@appointment.availability_slot.ends_at, format: :default, locale: I18n.locale) %>

<%= t('appointment_mailer.no_show_email.instructions') %>

<%= t('appointment_mailer.no_show_email.note') %>

<%= t('appointment_mailer.no_show_email.closing') %>

<%= t('appointment_mailer.common.signature') %> 